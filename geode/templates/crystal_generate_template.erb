# Crystal: <%= @crystal_name %> - Write what the crystal does here.
module Bot::<%= @crystal_name %>
<%= "  extend Discordrb::Commands::CommandContainer\n" if @containers[:command] -%>
<%= "  extend Discordrb::EventContainer\n" if @containers[:event] -%>
  
  # Crystal code below:
  
end